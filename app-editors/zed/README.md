# Zed

Zed is a fast, collaborative code editor.

## Upstream
- https://zed.dev
- https://github.com/zed-industries/zed

## USE flags
- `clang` (default on): builds using the Clang toolchain (disable with `-clang` to use GCC).
- `lto` (default on): enables LTO (thin LTO for Clang, full for GCC).